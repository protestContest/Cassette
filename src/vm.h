#pragma once
#include "value.h"
#include "chunk.h"

// typedef struct {
//   bool trace;
//   u32 sp;
//   Val *stack;
//   Chunk *chunk;
// } VM;

// VM *NewVM(void);
// void PrintRegisters(VM *vm);
// void Run(VM *vm, Chunk *chunk);
