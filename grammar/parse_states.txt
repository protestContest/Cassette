State 0
  program         → · stmts $
  stmts           → · stmts stmt NL
  stmts           → · stmts stmt $
  stmts           → · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On stmts → 1
    On nl    → 2
    On NL    → 3
    On $     → 4
    Reduce 89 (nl, 0)

State 1
  stmts           → stmts · stmt $
  stmts           → stmts · stmt NL
  program         → stmts · $
  stmt            → · let_stmt
  stmt            → · def_stmt
  stmt            → · import_stmt
  stmt            → · prim_call
  stmt            → · call
  let_stmt        → · "let" nl assigns
  def_stmt        → · "def" "(" params ")" do_block
  import_stmt     → · "import" STR
  import_stmt     → · "import" STR "as" ID
  prim_call       → · "@" ID call
  call            → · call arg
  call            → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On stmt        → 5
    On $           → 6
    On let_stmt    → 7
    On def_stmt    → 8
    On import_stmt → 9
    On prim_call   → 10
    On call        → 11
    On "let"       → 12
    On "def"       → 13
    On "import"    → 14
    On "@"         → 15
    On arg         → 16
    On lambda      → 17
    On "("         → 18
    On group       → 19
    On ID          → 20
    On logic       → 21
    On equals      → 22
    On compare     → 23
    On sum         → 24
    On product     → 25
    On block       → 26
    On do_block    → 27
    On if_block    → 28
    On cond_block  → 29
    On primary     → 30
    On "do"        → 31
    On "if"        → 32
    On "cond"      → 33
    On NUM         → 34
    On string      → 35
    On literal     → 36
    On symbol      → 37
    On list        → 38
    On tuple       → 39
    On dict        → 40
    On access      → 41
    On STR         → 42
    On "true"      → 43
    On "false"     → 44
    On "nil"       → 45
    On ":"         → 46
    On "["         → 47
    On "#["        → 48
    On "{"         → 49

State 2
  stmts           → nl ·
    Reduce 4 (stmts, 1)

State 3
  nl              → NL ·
    Reduce 87 (nl, 1)

State 4
  nl              → $ ·
    Reduce 88 (nl, 1)

State 5
  stmts           → stmts stmt · NL
  stmts           → stmts stmt · $
    On NL → 50
    On $  → 51

State 6
  program         → stmts $ ·
    Reduce 1 (program, 2)

State 7
  stmt            → let_stmt ·
    Reduce 5 (stmt, 1)

State 8
  stmt            → def_stmt ·
    Reduce 6 (stmt, 1)

State 9
  stmt            → import_stmt ·
    Reduce 7 (stmt, 1)

State 10
  stmt            → prim_call ·
    Reduce 8 (stmt, 1)

State 11
  call            → call · arg
  stmt            → call ·
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 52
    On lambda     → 17
    On "("        → 18
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 32
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49
    Reduce 9 (stmt, 1)

State 12
  let_stmt        → "let" · nl assigns
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 53
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 13
  def_stmt        → "def" · "(" params ")" do_block
    On "(" → 54

State 14
  import_stmt     → "import" · STR "as" ID
  import_stmt     → "import" · STR
    On STR → 55

State 15
  prim_call       → "@" · ID call
    On ID → 56

State 16
  call            → arg ·
    Reduce 23 (call, 1)

State 17
  arg             → lambda ·
    Reduce 26 (arg, 1)

State 18
  group           → "(" · ml_call ")"
  lambda          → "(" · ")" "->" nl logic
  ml_call         → · ml_call nl arg
  ml_call         → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On ml_call    → 57
    On ")"        → 58
    On arg        → 59
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 32
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 19
  primary         → group ·
  lambda          → group · "->" nl logic
    On "->" → 61
    Reduce 64 (primary, 1)

State 20
  access          → ID · "." ID
  primary         → ID ·
  lambda          → ID · "->" nl logic
    On "." → 62
    On "->" → 63
    Reduce 60 (primary, 1)

State 21
  logic           → logic · "or" nl equals
  logic           → logic · "and" nl equals
  lambda          → logic ·
    On "or" → 64
    On "and" → 65
    Reduce 30 (lambda, 1)

State 22
  logic           → equals ·
    Reduce 33 (logic, 1)

State 23
  equals          → compare ·
  equals          → compare · "!=" nl compare
  equals          → compare · "==" nl compare
    On "!=" → 66
    On "==" → 67
    Reduce 36 (equals, 1)

State 24
  sum             → sum · "-" nl product
  sum             → sum · "+" nl product
  compare         → sum ·
  compare         → sum · ">=" nl sum
  compare         → sum · ">" nl sum
  compare         → sum · "<=" nl sum
  compare         → sum · "<" nl sum
    On "-" → 68
    On "+" → 69
    On ">=" → 70
    On ">" → 71
    On "<=" → 72
    On "<" → 73
    Reduce 41 (compare, 1)

State 25
  product         → product · "/" nl block
  product         → product · "*" nl block
  sum             → product ·
    On "/" → 74
    On "*" → 75
    Reduce 44 (sum, 1)

State 26
  product         → block ·
    Reduce 47 (product, 1)

State 27
  block           → do_block ·
    Reduce 48 (block, 1)

State 28
  block           → if_block ·
    Reduce 49 (block, 1)

State 29
  block           → cond_block ·
    Reduce 50 (block, 1)

State 30
  block           → primary ·
    Reduce 51 (block, 1)

State 31
  do_block        → "do" · stmts opt_stmt "end"
  stmts           → · stmts stmt NL
  stmts           → · stmts stmt $
  stmts           → · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On stmts → 76
    On nl    → 2
    On NL    → 3
    On $     → 4
    Reduce 89 (nl, 0)

State 32
  if_block        → "if" · arg do_block
  if_block        → "if" · arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 77
    On lambda     → 17
    On "("        → 18
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 33
  cond_block      → "cond" · "do" clauses "end"
    On "do" → 79

State 34
  primary         → NUM ·
    Reduce 59 (primary, 1)

State 35
  primary         → string ·
    Reduce 61 (primary, 1)

State 36
  primary         → literal ·
    Reduce 62 (primary, 1)

State 37
  primary         → symbol ·
    Reduce 63 (primary, 1)

State 38
  primary         → list ·
    Reduce 65 (primary, 1)

State 39
  primary         → tuple ·
    Reduce 66 (primary, 1)

State 40
  primary         → dict ·
    Reduce 67 (primary, 1)

State 41
  access          → access · "." ID
  primary         → access ·
    On "." → 80
    Reduce 68 (primary, 1)

State 42
  string          → STR ·
    Reduce 69 (string, 1)

State 43
  literal         → "true" ·
    Reduce 72 (literal, 1)

State 44
  literal         → "false" ·
    Reduce 73 (literal, 1)

State 45
  literal         → "nil" ·
    Reduce 74 (literal, 1)

State 46
  symbol          → ":" · ID
    On ID → 81

State 47
  list            → "[" · items update "]"
  items           → · items arg opt_comma nl
  items           → · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On items → 82
    On nl    → 83
    On NL    → 3
    On $     → 4
    Reduce 89 (nl, 0)

State 48
  tuple           → "#[" · items "]"
  items           → · items arg opt_comma nl
  items           → · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On items → 84
    On nl    → 83
    On NL    → 3
    On $     → 4
    Reduce 89 (nl, 0)

State 49
  dict            → "{" · entries update "}"
  entries         → · entries entry opt_comma nl
  entries         → · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On entries → 85
    On nl      → 86
    On NL      → 3
    On $       → 4
    Reduce 89 (nl, 0)

State 50
  stmts           → stmts stmt NL ·
    Reduce 2 (stmts, 3)

State 51
  stmts           → stmts stmt $ ·
    Reduce 3 (stmts, 3)

State 52
  call            → call arg ·
    Reduce 22 (call, 2)

State 53
  let_stmt        → "let" nl · assigns
  assigns         → · assigns "," nl assign
  assigns         → · assign
  assign          → · ID "=" call
    On assigns → 87
    On assign  → 88
    On ID      → 89

State 54
  def_stmt        → "def" "(" · params ")" do_block
  params          → · params ID
  params          → · ID
    On params → 90
    On ID     → 91

State 55
  import_stmt     → "import" STR ·
  import_stmt     → "import" STR · "as" ID
    On "as" → 92
    Reduce 19 (import_stmt, 2)

State 56
  prim_call       → "@" ID · call
  call            → · call arg
  call            → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On call       → 93
    On arg        → 16
    On lambda     → 17
    On "("        → 18
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 32
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 57
  ml_call         → ml_call · nl arg
  group           → "(" ml_call · ")"
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 94
    On ")" → 95
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 58
  lambda          → "(" ")" · "->" nl logic
    On "->" → 96

State 59
  ml_call         → arg ·
    Reduce 25 (ml_call, 1)

State 60
  group           → "(" · ml_call ")"
  lambda          → "(" · ")" "->" nl logic
  ml_call         → · ml_call nl arg
  ml_call         → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On ml_call    → 57
    On ")"        → 58
    On arg        → 59
    On lambda     → 17
    On "("        → 18
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 32
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 61
  lambda          → group "->" · nl logic
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 97
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 62
  access          → ID "." · ID
    On ID → 98

State 63
  lambda          → ID "->" · nl logic
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 99
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 64
  logic           → logic "or" · nl equals
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 100
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 65
  logic           → logic "and" · nl equals
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 101
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 66
  equals          → compare "!=" · nl compare
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 102
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 67
  equals          → compare "==" · nl compare
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 103
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 68
  sum             → sum "-" · nl product
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 104
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 69
  sum             → sum "+" · nl product
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 105
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 70
  compare         → sum ">=" · nl sum
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 106
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 71
  compare         → sum ">" · nl sum
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 107
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 72
  compare         → sum "<=" · nl sum
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 108
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 73
  compare         → sum "<" · nl sum
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 109
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 74
  product         → product "/" · nl block
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 110
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 75
  product         → product "*" · nl block
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 111
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 76
  stmts           → stmts · stmt $
  stmts           → stmts · stmt NL
  do_block        → "do" stmts · opt_stmt "end"
  stmt            → · let_stmt
  stmt            → · def_stmt
  stmt            → · import_stmt
  stmt            → · prim_call
  stmt            → · call
  opt_stmt        → · stmt
  opt_stmt        → ε
  let_stmt        → · "let" nl assigns
  def_stmt        → · "def" "(" params ")" do_block
  import_stmt     → · "import" STR
  import_stmt     → · "import" STR "as" ID
  prim_call       → · "@" ID call
  call            → · call arg
  call            → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On stmt        → 112
    On opt_stmt    → 113
    On let_stmt    → 7
    On def_stmt    → 8
    On import_stmt → 9
    On prim_call   → 10
    On call        → 11
    On "let"       → 12
    On "def"       → 13
    On "import"    → 14
    On "@"         → 15
    On arg         → 16
    On lambda      → 17
    On "("         → 60
    On group       → 19
    On ID          → 20
    On logic       → 21
    On equals      → 22
    On compare     → 23
    On sum         → 24
    On product     → 25
    On block       → 26
    On do_block    → 27
    On if_block    → 28
    On cond_block  → 29
    On primary     → 30
    On "do"        → 31
    On "if"        → 32
    On "cond"      → 33
    On NUM         → 34
    On string      → 35
    On literal     → 36
    On symbol      → 37
    On list        → 38
    On tuple       → 39
    On dict        → 40
    On access      → 41
    On STR         → 42
    On "true"      → 43
    On "false"     → 44
    On "nil"       → 45
    On ":"         → 46
    On "["         → 47
    On "#["        → 48
    On "{"         → 49
    Reduce 11 (opt_stmt, 0)

State 77
  if_block        → "if" arg · "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → "if" arg · do_block
  do_block        → · "do" stmts opt_stmt "end"
    On "do"     → 114
    On do_block → 115

State 78
  if_block        → "if" · arg do_block
  if_block        → "if" · arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 77
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 32
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 79
  cond_block      → "cond" "do" · clauses "end"
  clauses         → · clauses clause
  clauses         → · NL
    On clauses → 116
    On NL      → 117

State 80
  access          → access "." · ID
    On ID → 118

State 81
  symbol          → ":" ID ·
    Reduce 75 (symbol, 2)

State 82
  items           → items · arg opt_comma nl
  list            → "[" items · update "]"
  arg             → · lambda
  update          → · "|" nl arg
  update          → ε
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 119
    On update     → 120
    On lambda     → 17
    On "|"        → 121
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49
    Reduce 86 (update, 0)

State 83
  items           → nl ·
    Reduce 79 (items, 1)

State 84
  items           → items · arg opt_comma nl
  tuple           → "#[" items · "]"
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 119
    On "]"        → 122
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 85
  entries         → entries · entry opt_comma nl
  dict            → "{" entries · update "}"
  entry           → · ID ":" nl arg
  update          → · "|" nl arg
  update          → ε
    On entry  → 123
    On update → 124
    On ID     → 125
    On "|"    → 121
    Reduce 86 (update, 0)

State 86
  entries         → nl ·
    Reduce 83 (entries, 1)

State 87
  assigns         → assigns · "," nl assign
  let_stmt        → "let" nl assigns ·
    On "," → 126
    Reduce 12 (let_stmt, 3)

State 88
  assigns         → assign ·
    Reduce 14 (assigns, 1)

State 89
  assign          → ID · "=" call
    On "=" → 127

State 90
  params          → params · ID
  def_stmt        → "def" "(" params · ")" do_block
    On ID → 128
    On ")" → 129

State 91
  params          → ID ·
    Reduce 18 (params, 1)

State 92
  import_stmt     → "import" STR "as" · ID
    On ID → 130

State 93
  call            → call · arg
  prim_call       → "@" ID call ·
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 52
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49
    Reduce 21 (prim_call, 3)

State 94
  ml_call         → ml_call nl · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 131
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 95
  group           → "(" ml_call ")" ·
    Reduce 76 (group, 3)

State 96
  lambda          → "(" ")" "->" · nl logic
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 132
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 97
  lambda          → group "->" nl · logic
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On logic      → 133
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 98
  access          → ID "." ID ·
    Reduce 71 (access, 3)

State 99
  lambda          → ID "->" nl · logic
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On logic      → 137
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 100
  logic           → logic "or" nl · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On equals     → 138
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 101
  logic           → logic "and" nl · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On equals     → 139
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 102
  equals          → compare "!=" nl · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On compare    → 140
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 103
  equals          → compare "==" nl · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On compare    → 141
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 104
  sum             → sum "-" nl · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On product    → 142
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 105
  sum             → sum "+" nl · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On product    → 143
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 106
  compare         → sum ">=" nl · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On sum        → 144
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 107
  compare         → sum ">" nl · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On sum        → 145
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 108
  compare         → sum "<=" nl · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On sum        → 146
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 109
  compare         → sum "<" nl · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On sum        → 147
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 110
  product         → product "/" nl · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On block      → 148
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 111
  product         → product "*" nl · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On block      → 149
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 112
  opt_stmt        → stmt ·
  stmts           → stmts stmt · NL
  stmts           → stmts stmt · $
    On NL → 50
    On $  → 51
    Reduce 10 (opt_stmt, 1)

State 113
  do_block        → "do" stmts opt_stmt · "end"
    On "end" → 150

State 114
  do_block        → "do" · stmts opt_stmt "end"
  if_block        → "if" arg "do" · stmts opt_stmt "else" stmts opt_stmt "end"
  stmts           → · stmts stmt NL
  stmts           → · stmts stmt $
  stmts           → · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On stmts → 151
    On nl    → 2
    On NL    → 3
    On $     → 4
    Reduce 89 (nl, 0)

State 115
  if_block        → "if" arg do_block ·
    Reduce 54 (if_block, 3)

State 116
  clauses         → clauses · clause
  cond_block      → "cond" "do" clauses · "end"
  clause          → · logic "->" nl call NL
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On clause     → 152
    On "end"      → 153
    On logic      → 154
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 117
  clauses         → NL ·
    Reduce 57 (clauses, 1)

State 118
  access          → access "." ID ·
    Reduce 70 (access, 3)

State 119
  items           → items arg · opt_comma nl
  opt_comma       → · ","
  opt_comma       → ε
    On opt_comma → 155
    On ","       → 156
    Reduce 91 (opt_comma, 0)

State 120
  list            → "[" items update · "]"
    On "]" → 157

State 121
  update          → "|" · nl arg
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 158
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 122
  tuple           → "#[" items "]" ·
    Reduce 80 (tuple, 3)

State 123
  entries         → entries entry · opt_comma nl
  opt_comma       → · ","
  opt_comma       → ε
    On opt_comma → 159
    On ","       → 156
    Reduce 91 (opt_comma, 0)

State 124
  dict            → "{" entries update · "}"
    On "}" → 160

State 125
  entry           → ID · ":" nl arg
    On ":" → 161

State 126
  assigns         → assigns "," · nl assign
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 162
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 127
  assign          → ID "=" · call
  call            → · call arg
  call            → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On call       → 163
    On arg        → 16
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 128
  params          → params ID ·
    Reduce 17 (params, 2)

State 129
  def_stmt        → "def" "(" params ")" · do_block
  do_block        → · "do" stmts opt_stmt "end"
    On do_block → 164
    On "do"     → 31

State 130
  import_stmt     → "import" STR "as" ID ·
    Reduce 20 (import_stmt, 4)

State 131
  ml_call         → ml_call nl arg ·
    Reduce 24 (ml_call, 3)

State 132
  lambda          → "(" ")" "->" nl · logic
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  group           → · "(" ml_call ")"
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On logic      → 165
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On ID         → 134
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On group      → 135
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "("        → 136
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 133
  logic           → logic · "or" nl equals
  logic           → logic · "and" nl equals
  lambda          → group "->" nl logic ·
    On "or" → 64
    On "and" → 65
    Reduce 28 (lambda, 4)

State 134
  access          → ID · "." ID
  primary         → ID ·
    On "." → 62
    Reduce 60 (primary, 1)

State 135
  primary         → group ·
    Reduce 64 (primary, 1)

State 136
  group           → "(" · ml_call ")"
  ml_call         → · ml_call nl arg
  ml_call         → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On ml_call    → 57
    On arg        → 59
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 137
  logic           → logic · "or" nl equals
  logic           → logic · "and" nl equals
  lambda          → ID "->" nl logic ·
    On "or" → 64
    On "and" → 65
    Reduce 29 (lambda, 4)

State 138
  logic           → logic "or" nl equals ·
    Reduce 32 (logic, 4)

State 139
  logic           → logic "and" nl equals ·
    Reduce 31 (logic, 4)

State 140
  equals          → compare "!=" nl compare ·
    Reduce 35 (equals, 4)

State 141
  equals          → compare "==" nl compare ·
    Reduce 34 (equals, 4)

State 142
  product         → product · "/" nl block
  product         → product · "*" nl block
  sum             → sum "-" nl product ·
    On "/" → 74
    On "*" → 75
    Reduce 43 (sum, 4)

State 143
  product         → product · "/" nl block
  product         → product · "*" nl block
  sum             → sum "+" nl product ·
    On "/" → 74
    On "*" → 75
    Reduce 42 (sum, 4)

State 144
  sum             → sum · "-" nl product
  sum             → sum · "+" nl product
  compare         → sum ">=" nl sum ·
    On "-" → 68
    On "+" → 69
    Reduce 40 (compare, 4)

State 145
  sum             → sum · "-" nl product
  sum             → sum · "+" nl product
  compare         → sum ">" nl sum ·
    On "-" → 68
    On "+" → 69
    Reduce 39 (compare, 4)

State 146
  sum             → sum · "-" nl product
  sum             → sum · "+" nl product
  compare         → sum "<=" nl sum ·
    On "-" → 68
    On "+" → 69
    Reduce 38 (compare, 4)

State 147
  sum             → sum · "-" nl product
  sum             → sum · "+" nl product
  compare         → sum "<" nl sum ·
    On "-" → 68
    On "+" → 69
    Reduce 37 (compare, 4)

State 148
  product         → product "/" nl block ·
    Reduce 46 (product, 4)

State 149
  product         → product "*" nl block ·
    Reduce 45 (product, 4)

State 150
  do_block        → "do" stmts opt_stmt "end" ·
    Reduce 52 (do_block, 4)

State 151
  stmts           → stmts · stmt $
  stmts           → stmts · stmt NL
  if_block        → "if" arg "do" stmts · opt_stmt "else" stmts opt_stmt "end"
  do_block        → "do" stmts · opt_stmt "end"
  stmt            → · let_stmt
  stmt            → · def_stmt
  stmt            → · import_stmt
  stmt            → · prim_call
  stmt            → · call
  opt_stmt        → · stmt
  opt_stmt        → ε
  let_stmt        → · "let" nl assigns
  def_stmt        → · "def" "(" params ")" do_block
  import_stmt     → · "import" STR
  import_stmt     → · "import" STR "as" ID
  prim_call       → · "@" ID call
  call            → · call arg
  call            → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On stmt        → 112
    On opt_stmt    → 166
    On let_stmt    → 7
    On def_stmt    → 8
    On import_stmt → 9
    On prim_call   → 10
    On call        → 11
    On "let"       → 12
    On "def"       → 13
    On "import"    → 14
    On "@"         → 15
    On arg         → 16
    On lambda      → 17
    On "("         → 60
    On group       → 19
    On ID          → 20
    On logic       → 21
    On equals      → 22
    On compare     → 23
    On sum         → 24
    On product     → 25
    On block       → 26
    On do_block    → 27
    On if_block    → 28
    On cond_block  → 29
    On primary     → 30
    On "do"        → 31
    On "if"        → 78
    On "cond"      → 33
    On NUM         → 34
    On string      → 35
    On literal     → 36
    On symbol      → 37
    On list        → 38
    On tuple       → 39
    On dict        → 40
    On access      → 41
    On STR         → 42
    On "true"      → 43
    On "false"     → 44
    On "nil"       → 45
    On ":"         → 46
    On "["         → 47
    On "#["        → 48
    On "{"         → 49
    Reduce 11 (opt_stmt, 0)

State 152
  clauses         → clauses clause ·
    Reduce 56 (clauses, 2)

State 153
  cond_block      → "cond" "do" clauses "end" ·
    Reduce 55 (cond_block, 4)

State 154
  logic           → logic · "or" nl equals
  logic           → logic · "and" nl equals
  clause          → logic · "->" nl call NL
    On "or" → 64
    On "and" → 65
    On "->" → 167

State 155
  items           → items arg opt_comma · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 168
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 156
  opt_comma       → "," ·
    Reduce 90 (opt_comma, 1)

State 157
  list            → "[" items update "]" ·
    Reduce 77 (list, 4)

State 158
  update          → "|" nl · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 169
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 159
  entries         → entries entry opt_comma · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 170
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 160
  dict            → "{" entries update "}" ·
    Reduce 81 (dict, 4)

State 161
  entry           → ID ":" · nl arg
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 171
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 162
  assigns         → assigns "," nl · assign
  assign          → · ID "=" call
    On assign → 172
    On ID     → 89

State 163
  call            → call · arg
  assign          → ID "=" call ·
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 52
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49
    Reduce 15 (assign, 3)

State 164
  def_stmt        → "def" "(" params ")" do_block ·
    Reduce 16 (def_stmt, 5)

State 165
  logic           → logic · "or" nl equals
  logic           → logic · "and" nl equals
  lambda          → "(" ")" "->" nl logic ·
    On "or" → 64
    On "and" → 65
    Reduce 27 (lambda, 5)

State 166
  do_block        → "do" stmts opt_stmt · "end"
  if_block        → "if" arg "do" stmts opt_stmt · "else" stmts opt_stmt "end"
    On "end" → 150
    On "else" → 173

State 167
  clause          → logic "->" · nl call NL
  nl              → · NL
  nl              → · $
  nl              → ε
    On nl → 174
    On NL → 3
    On $  → 4
    Reduce 89 (nl, 0)

State 168
  items           → items arg opt_comma nl ·
    Reduce 78 (items, 4)

State 169
  update          → "|" nl arg ·
    Reduce 85 (update, 3)

State 170
  entries         → entries entry opt_comma nl ·
    Reduce 82 (entries, 4)

State 171
  entry           → ID ":" nl · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 175
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 172
  assigns         → assigns "," nl assign ·
    Reduce 13 (assigns, 4)

State 173
  if_block        → "if" arg "do" stmts opt_stmt "else" · stmts opt_stmt "end"
  stmts           → · stmts stmt NL
  stmts           → · stmts stmt $
  stmts           → · nl
  nl              → · NL
  nl              → · $
  nl              → ε
    On stmts → 176
    On nl    → 2
    On NL    → 3
    On $     → 4
    Reduce 89 (nl, 0)

State 174
  clause          → logic "->" nl · call NL
  call            → · call arg
  call            → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On call       → 177
    On arg        → 16
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 175
  entry           → ID ":" nl arg ·
    Reduce 84 (entry, 4)

State 176
  stmts           → stmts · stmt $
  stmts           → stmts · stmt NL
  if_block        → "if" arg "do" stmts opt_stmt "else" stmts · opt_stmt "end"
  stmt            → · let_stmt
  stmt            → · def_stmt
  stmt            → · import_stmt
  stmt            → · prim_call
  stmt            → · call
  opt_stmt        → · stmt
  opt_stmt        → ε
  let_stmt        → · "let" nl assigns
  def_stmt        → · "def" "(" params ")" do_block
  import_stmt     → · "import" STR
  import_stmt     → · "import" STR "as" ID
  prim_call       → · "@" ID call
  call            → · call arg
  call            → · arg
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On stmt        → 112
    On opt_stmt    → 178
    On let_stmt    → 7
    On def_stmt    → 8
    On import_stmt → 9
    On prim_call   → 10
    On call        → 11
    On "let"       → 12
    On "def"       → 13
    On "import"    → 14
    On "@"         → 15
    On arg         → 16
    On lambda      → 17
    On "("         → 60
    On group       → 19
    On ID          → 20
    On logic       → 21
    On equals      → 22
    On compare     → 23
    On sum         → 24
    On product     → 25
    On block       → 26
    On do_block    → 27
    On if_block    → 28
    On cond_block  → 29
    On primary     → 30
    On "do"        → 31
    On "if"        → 78
    On "cond"      → 33
    On NUM         → 34
    On string      → 35
    On literal     → 36
    On symbol      → 37
    On list        → 38
    On tuple       → 39
    On dict        → 40
    On access      → 41
    On STR         → 42
    On "true"      → 43
    On "false"     → 44
    On "nil"       → 45
    On ":"         → 46
    On "["         → 47
    On "#["        → 48
    On "{"         → 49
    Reduce 11 (opt_stmt, 0)

State 177
  call            → call · arg
  clause          → logic "->" nl call · NL
  arg             → · lambda
  lambda          → · "(" ")" "->" nl logic
  lambda          → · group "->" nl logic
  lambda          → · ID "->" nl logic
  lambda          → · logic
  group           → · "(" ml_call ")"
  logic           → · logic "and" nl equals
  logic           → · logic "or" nl equals
  logic           → · equals
  equals          → · compare "==" nl compare
  equals          → · compare "!=" nl compare
  equals          → · compare
  compare         → · sum "<" nl sum
  compare         → · sum "<=" nl sum
  compare         → · sum ">" nl sum
  compare         → · sum ">=" nl sum
  compare         → · sum
  sum             → · sum "+" nl product
  sum             → · sum "-" nl product
  sum             → · product
  product         → · product "*" nl block
  product         → · product "/" nl block
  product         → · block
  block           → · do_block
  block           → · if_block
  block           → · cond_block
  block           → · primary
  do_block        → · "do" stmts opt_stmt "end"
  if_block        → · "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end"
  if_block        → · "if" arg do_block
  cond_block      → · "cond" "do" clauses "end"
  primary         → · NUM
  primary         → · ID
  primary         → · string
  primary         → · literal
  primary         → · symbol
  primary         → · group
  primary         → · list
  primary         → · tuple
  primary         → · dict
  primary         → · access
  string          → · STR
  literal         → · "true"
  literal         → · "false"
  literal         → · "nil"
  symbol          → · ":" ID
  list            → · "[" items update "]"
  tuple           → · "#[" items "]"
  dict            → · "{" entries update "}"
  access          → · access "." ID
  access          → · ID "." ID
    On arg        → 52
    On NL         → 179
    On lambda     → 17
    On "("        → 60
    On group      → 19
    On ID         → 20
    On logic      → 21
    On equals     → 22
    On compare    → 23
    On sum        → 24
    On product    → 25
    On block      → 26
    On do_block   → 27
    On if_block   → 28
    On cond_block → 29
    On primary    → 30
    On "do"       → 31
    On "if"       → 78
    On "cond"     → 33
    On NUM        → 34
    On string     → 35
    On literal    → 36
    On symbol     → 37
    On list       → 38
    On tuple      → 39
    On dict       → 40
    On access     → 41
    On STR        → 42
    On "true"     → 43
    On "false"    → 44
    On "nil"      → 45
    On ":"        → 46
    On "["        → 47
    On "#["       → 48
    On "{"        → 49

State 178
  if_block        → "if" arg "do" stmts opt_stmt "else" stmts opt_stmt · "end"
    On "end" → 180

State 179
  clause          → logic "->" nl call NL ·
    Reduce 58 (clause, 5)

State 180
  if_block        → "if" arg "do" stmts opt_stmt "else" stmts opt_stmt "end" ·
    Reduce 53 (if_block, 9)

