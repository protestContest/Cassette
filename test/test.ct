import File
import IO

; def (stream-chunks file bufsize) do
;   let data = (File.read! file bufsize)
;   IO.print data
;   IO.print "---"
;   data | \-> (stream-chunks file bufsize)
; end

; let file = File.open! "/Users/zack/Desktop/seattle.asc" "r"

IO.print ["abc", 0x0A]
