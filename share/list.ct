module List
export reduce, reverse

def reduce(list, acc, fn) do
  if list do
    reduce(list[1:], fn(list[0], acc), fn)
  else
    acc
  end
end

def reverse(list) do
  reduce(list, nil, \item, tail -> item | tail)
end
