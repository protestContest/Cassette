module Enum

def (last-index-of item enum) do
  def (loop index) do
    cond do
      index < 0 -> #enum
      enum.(index) == item -> index
      true -> loop index - 1
    end
  end

  loop #enum - 1
end
