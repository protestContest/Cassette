module Map

def get(map, key) map-get(map, key)
def put(map, key, value) map-set(map, key, value)
def put-new(map, key, value) do
  if key in map do
    map
  else
    put(map, key, value)
  end
end
def delete(map, key) map-del(map, key)
